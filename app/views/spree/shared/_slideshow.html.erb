<% if defined?(images) && !images.nil? %>
  <ul class="bxslider">
    <% images.each do |image| %>
      <li><%= image_tag(main_app.url_for(@image.url(size)), alt: 'image.alt') %></li>
    <% end %>
  </div>
<% end %>